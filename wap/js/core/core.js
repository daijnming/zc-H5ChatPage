
var Core = function(){
    var that = {};
    // var initConfig = require('../listMsg/initConfig.js');
    // var comm = require('../util/comm.js');
    // that.initConfig = initConfig;
    // that.comm = comm;
    return that;
};
module.exports = Core;
