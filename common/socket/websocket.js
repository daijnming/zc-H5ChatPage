/**
 * @author Treagzhao
 */
function WebSocket(puid) {
    this.puid = puid;
    console.log(WebSocket);
    var start = function() {
    };

    var stop = function() {
    };

    this.start = start;
    this.stop = stop;
};

module.exports = WebSocket;
