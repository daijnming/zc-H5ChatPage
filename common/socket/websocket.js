/**
 * @author Treagzhao
 */
function WebSocket(puid) {
    this.puid = puid;

    var destroy = function() {
    };

    var start = function() {
    };

    var stop = function() {
    };

    this.destroy = destroy;
    this.start = start;
    this.stop = stop;
};

module.exports = WebSocket;
